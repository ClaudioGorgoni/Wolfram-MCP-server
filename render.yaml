services:
  - type: web
    name: wolfram-mcp-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: WOLFRAM_API_KEY
        sync: false  # Gardez cette valeur secrète
    plan: free